linters:
  presets:
    - bugs
    - performance
    - unused
    - format
  disable:
    - musttag
  enable:
    - whitespace
    - misspell
    - dupl

linters-settings:
  exhaustive:
    default-signifies-exhaustive: true
